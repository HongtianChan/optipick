2026-3-10

**课上遗留问题（待后续讨论）**
- 如何证明答案是对的？（正确性：覆盖条件是否全部满足，可用 checker 程序验证）
- 如何证明答案是最优的？（最优性：回溯穷举可证；贪心只能说是可行解，不能证最优）

**黑板笔记（老师写的，与证明相关）**
- 解的误差表示：`|g(n) − g*(n)| < ε`，其中 g(n) = 我们的解（Soln），g*(n) = 最优解（opt. Soln）。老师写了容忍度 **±5%**、**±2**（相对或绝对误差在可接受范围内即可说明「接近最优」）。
- 误差界形式：`|| … || ≤ (f'(n) = f(n) + kε)`，即计算结果 f' 与理论/真实 f 的差有上界 kε，用于说明正确性或误差可控。
- 和项目的对应：正确性用 checker 验证；最优性/近似质量在报告里可写「小规模回溯得最优 g = g*，大规模贪心满足 |g − g*| 在 ±5% 或 ±2 内」。

---

2026-4-8

**本次补齐（对照 Group Project 提交要求）**

1. **输入合法性（后端硬校验）**
- 文件：`api/solve.js`
- 新增 `validateInputs()`，对 `m,n,k,j,s,atLeast` 做正整数校验。
- 新增范围与关系校验：
  - `45 <= m <= 54`
  - `7 <= n <= 25`
  - `4 <= k <= 7`
  - `3 <= s <= 7`
  - `n <= m`, `k <= n`, `s <= j <= k`, `j <= n`
- 手动输入 `samples` 增加校验：必须是数组、长度等于 `n`、元素为正整数、范围在 `[1,m]`、且不重复。
- 目的：避免 demo 时因异常输入导致错误结果或崩溃。

2. **DB 交付增强（可导出提交）**
- 新增文件：`api/export.js`
- 功能：从 Supabase `results` 表读取历史记录并导出 JSON（含 `file_name,m,n,k,j,s,samples,groups,count,run_count,created_at`）。
- 前端文件：`index.html`
- 在 History 区新增 `Export DB` 按钮，点击后下载 `db-export-*.json`。
- 目的：方便 USB 提交“DB files / sample run outputs”。

3. **数据库约束补齐**
- 文件：`database/supabase-setup.sql`
- `results` 表增加 CHECK 约束：
  - `m,n,k,j,s` 的范围约束
  - `n <= m`, `k <= n`, `j <= k`, `s <= j`
- 目的：从数据库层防止脏数据写入。

4. **提交物分类目录**
- 新增目录与文件：
  - `submission/README.md`
  - `submission/CHECKLIST.md`
  - `submission/source-code/.gitkeep`
  - `submission/db/.gitkeep`
  - `submission/sample-runs/.gitkeep`
  - `submission/reports/.gitkeep`
  - `submission/presentation/.gitkeep`
- 目的：按 Week 15 提交结构提前整理，减少临交前遗漏。

5. **文档同步**
- 文件：`README.md`
- 新增内容：
  - 支持 DB JSON 导出说明
  - 输入校验规则说明
  - History 导出使用说明

**本次结果**
- 已完成：输入合法性补齐、DB 导出能力、提交目录模板。
- 待组内补充：`User-Manual.pdf`、`Project-Report.pdf`、`sample-runs` 实际截图与导出文件。
### 2026-4-8 (下午)

- **算法升级 (GRASP)**: 将 `api/algorithm.js` 中的单次贪心算法升级为 **GRASP (Greedy Randomized Adaptive Search Procedure)**。
  - **原因**: 用户反馈在较大输入（如 `n=15, k=6`）时，原有的单次贪心算法运行时间长且结果不够优化。
  - **实现**: 引入了时间预算（3.5秒，以适应 Vercel 的 10 秒限制），在预算内多次运行随机化的贪心算法，每次在局部最优解附近加入随机性以跳出局部最优，最终返回所有迭代中找到的最小集合。
  - **效果**: 在保证响应时间的前提下，能找到比单次贪心更小的组数（更接近最优解），满足项目要求中“生成最优/接近最优解”且“执行时间短且结果好”的加分项。


### 2026-4-8 (下午，操作明细)

- 本次是直接在现有代码上做增量升级，不是重写算法。
- 具体改动文件：`api/algorithm.js`。
- 关键操作：
  - 把原来“单次 greedy”改成“限时多次迭代的 GRASP”（`greedySetCover(..., timeLimitMs)`）。
  - 增加 `startTime` + `while (Date.now() - startTime < timeLimitMs)` 控制总时长。
  - 每轮从“当前新增覆盖最多”的候选里随机选一个，避免每次走同一路径。
  - 每轮结束后更新 `globalBest`，最终返回预算时间内最小解。
  - 大规模分支里 method 从 `greedy` 改为 `grasp`，便于前端展示和报告说明。
  - 保留 `removeRedundantGroups` 做后处理，继续压缩冗余组。
- 工程约束：
  - GRASP 预算设置为 `3500ms`，给 Vercel 超时留安全余量。
  - 小规模仍走 `backtrack` 精确解（`C(n,k) <= 300`），保证小例子可给最优。
- 版本记录：
  - `a9dff3a`：`feat: upgrade greedy algorithm to GRASP for better results on large inputs`
  - `8cb78d6`：`docs: log GRASP algorithm upgrade`


### 2026-4-8 (下午，参数选择说明)

- **为什么是 3500ms，不是更长？**
  - Vercel 免费函数有执行时限，算法不是唯一耗时，还要预留网络传输、JSON 序列化、数据库写入时间。
  - 设为 `3500ms` 是工程折中：既能跑多轮 GRASP，又能显著降低超时风险。
- **为什么不是更短（比如 500ms）？**
  - 太短会导致迭代次数不足，随机搜索优势发挥不出来，结果容易退化到接近单次贪心。
- **当前策略的目标**
  - 小规模优先最优：`C(n,k) <= 300` 用 `backtrack` 保证最优。
  - 大规模优先稳态：限时 GRASP 在可控时间内尽量逼近最优。
- **答辩可用**
  - “我们把算法做成双模式：小规模给精确最优，大规模给限时近似最优；3500ms 是在云函数时限下平衡结果质量与稳定性的工程参数。”

### 2026-4-14（本地模式与前端目录迁移）

- 目标：支持“每位组员本机独立运行 + 本机独立存储”，避免依赖团队后端数据库。
- 本地模式增强：
  - 新增 `docs/local-mode.md`，提供离线运行步骤与数据目录说明。
  - 根目录 `package.json` 新增脚本：`local:web`、`local:solve`。
  - `cli/src/server.js` 新增本地 `GET /api/export`，History 的 Export DB 可在本地模式正常导出。
- 路径与结构修正：
  - 将前端主页面从根目录迁移到 `web-ui/index.html`。
  - 更新 `vercel.json` rewrites 到 `/web-ui/index.html`，线上路由与新目录一致。
  - 本地 server 静态页面读取路径同步，优先读取 `web-ui/index.html`。
- Bug 修复（本地启动）：
  - 修复 `cli/index.js` 中端口解析导致 `NaN` 的问题（`ERR_SOCKET_BAD_PORT`）。
  - 修复本地 server 找不到页面文件导致崩溃（`ENOENT ... cli/web/index.html`）。
- 验证结果：
  - 本地 `npm run local:web` 可正常启动。
  - `http://localhost:3000` 可访问页面，`/api/solve` 返回正常计算结果。

### 2026-4-14（算法性能优化 + 项目工程优化）

#### 一、算法优化（面向大规模参数）

- 问题定位：在 `m=50/54, n=20, k=6, j=6, s=5` 场景下，组合空间 `C(20,6)=38760`，原先会出现 50s 级别耗时。
- 核心优化：`api/algorithm.js`
  - 引入大规模场景的 **随机候选采样贪心（stochastic scan）**，每轮不再全量扫描全部候选集合。
  - 对超大候选空间跳过高成本的覆盖等价去重步骤（dedup-by-coverage），优先保证总时延受控。
  - 保留 GRASP 时间预算搜索策略，并继续保证覆盖约束检查逻辑一致。
- 本地/线上一致性：`cli/src/algorithm.js` 改为直接复用 `api/algorithm.js`，避免算法分叉。
- 效果验证：
  - 优化前：`n=20` 典型耗时约 50s。
  - 优化后：`n=20` 典型耗时约 5s（约 10x 级加速）。
  - 覆盖正确性仍满足（证据报告显示 `38760/38760` 覆盖，100%）。

#### 二、项目工程优化（可用性与交付稳定性）

- 本地离线模式完善：
  - 新增 `docs/local-mode.md`，明确每位组员本机独立运行、独立存储。
  - 新增 `npm run local:web` / `npm run local:solve`。
  - 本地 server 增加 `GET /api/export`，支持本地 History 导出 JSON。
- 前端目录结构优化：
  - 前端入口从根目录迁移到 `web-ui/index.html`。
  - `vercel.json` 与本地 server 静态路径同步更新，消除路径错位问题。
- 本地启动问题修复：
  - 修复端口解析导致 `ERR_SOCKET_BAD_PORT`。
  - 修复本地读取页面路径导致 `ENOENT` 崩溃。
- 交互体验优化：
  - 进度条从“模拟进度”改为“有意义进度”：
    - 大规模 GRASP 显示基于时间预算的真实百分比；
    - 小规模精确搜索显示动态状态 + 已耗时。
  - 保存历史逻辑优化：`Save to history` 支持传入预计算结果，避免二次计算导致的长等待。

#### 三、证据与产出

- 新增可解释证据脚本：`scripts/generate-evidence-report.js`
  - 自动输出 Markdown 证据报告（覆盖数量、覆盖率、运行时间、组数统计、最优/近优说明）。
- 生成示例：`submission/sample-runs/evidence-n20-fast.md`
  - 显示大规模场景下优化后运行时间与覆盖正确性证据。

### 2026-4-14（提交收尾：USB 清单与报告图表规范化）

#### 一、交付收尾

- 新增 `submission/USB-CHECKLIST.md`，将 Week 15 USB 封包要求拆成可勾选项（源码、DB、样例输出、报告 PDF、PPT、终检）。
- 更新 `submission/README.md`，加入清单入口（`USB-CHECKLIST.md` / `CHECKLIST.md` / `TODO-Week15.md`），方便组员协作。

#### 二、报告图表补齐（满足 method/methodology 图示要求）

- 更新 `submission/reports/Project-Report.md` 与 `submission/reports/Project-Report.zh-Hant.md`：
  - 新增程序流程图（Program Flowchart）
  - 新增系统流程图（System Flowchart）
  - 新增 UI 状态图（State Diagram）
  - 新增 API 时序图（Sequence Diagram）
  - 新增 simple example（小规模精确解示例）

#### 三、图表质量修正（打印友好）

- 将系统流程图改为自上而下（TD）布局，降低横向拥挤与交叉线。
- 修正 `/api/solve` 连线语义：
  - 计算链路：`/api/solve <-> algorithm.js`
  - 存储链路：仅 `save=true` 时写库
  - 返回链路：结果回到 UI，不再使用含糊的全局 DB->UI 直连
- 状态图精简为可落地状态集合（Inputting/Solving/ResultReady/Saving/Saved），避免“条件当状态”的歧义。

#### 四、当前状态

- 代码层与方法图示层已基本完成，可进入最终数据补全与 PDF 定稿阶段（组号/学号/实验表格 `<fill>` 待最终填写）。
