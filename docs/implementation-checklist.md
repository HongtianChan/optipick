# 实现检查清单

对照 Group Project 说明，检查当前实现完整性。

## ✅ 已实现功能

### 1. 用户界面
- [x] 系统标题：`An Optimal Samples Selection System`
- [x] 用户友好界面（终端风格）

### 2. 参数输入
- [x] m, n, k, j, s 输入框
- [x] 范围提示（45 <= m <= 54 等）
- [x] at least 参数输入
- [x] 使用数字（1-54），不用字母

### 3. 样本选择方式
- [x] 随机模式（Random n）
- [x] 手动输入模式（Input n）
- [x] 选中的 n 个数字显示在屏幕上

### 4. 结果存储
- [x] 输出到 DB 文件（Supabase）
- [x] 文件名格式：`m-n-k-j-s-x-y`
- [x] x = 运行次数，y = 结果数量

### 5. 数据库操作
- [x] 显示：从 DB 文件读取并显示结果
- [x] 删除：删除 DB 文件中的记录
- [x] 文件列表显示

### 6. UI 组件

#### 主界面
- [x] 参数输入区（m, n, k, j, s, at least）
- [x] 模式选择（Random n / Input n）
- [x] 操作按钮（execute, store, clear）
- [x] Values input 区域（显示格式：1st # 5, 2nd # 12...）
- [x] User input 区域（编号输入框 1-25）
- [x] results 区域（显示计算结果）
- [x] 文件名显示

#### 数据库界面
- [x] 文件列表（File List）
- [x] Display 按钮
- [x] Delete 按钮
- [x] 结果显示区
- [x] back 按钮
- [x] print 按钮

## ⚠️ 需要确认/改进

### 1. 结果显示格式
**要求**：每个结果是一个数字（代表一个 k=6 组）
- 例如：54, 55, 54, ...（这些数字代表不同的 k=6 组）

**当前实现**：显示为 `[1, 2, 3, 4, 5, 6]` 数组格式

**问题**：需要确认：
- 数字代表什么？是组的索引还是其他编码？
- 是否需要将数组转换为数字格式？

**建议**：如果 PDF 中显示的是数字，可能需要：
- 将每个 k=6 组编码为一个数字（如组合索引）
- 或显示组的编号（1, 2, 3...）

### 2. 分页功能
**要求**：如果结果很多，有 "NEXT" 按钮查看下一页

**当前实现**：所有结果显示在一个文本框中

**建议**：如果结果数量很大（>50），可以考虑分页显示

### 3. Values input 编辑
**要求**：如果选"Input n"，用户可以在这里手动输入 n 个数字

**当前实现**：Values input 是只读的，用户需要在 User input 区域输入

**状态**：符合要求（User input 输入后显示在 Values input）

### 4. 文件列表选择
**要求**：从文件列表中选择一个 DB 文件（点击文件名）

**当前实现**：需要确认点击文件名是否选中

### 5. 结果数量验证
**要求**：运行结果示例，结果和 pdf 给的不要差太远，不要太多结果

**需要**：测试所有示例（E.g. 1-9），确保结果数量接近 PDF 中的答案

## 📋 待测试示例

根据 Group Project 说明，需要验证以下示例：

| 示例 | m | n | k | j | s | 预期结果 | 状态 |
|------|---|---|---|---|---|--------|------|
| E.g. 1 | 45 | 7 | 6 | 5 | 5 | 最少 6 组 | ⏳ |
| E.g. 2 | 45 | 8 | 6 | 5 | 5 | 最少 12 组 | ⏳ |
| E.g. 3 | 45 | 8 | 6 | 4 | 4 | 最少 7 组 | ⏳ |
| E.g. 4 | 45 | 9 | 6 | 4 | 4 | 最少 12 组 | ⏳ |
| E.g. 5 | 45 | 8 | 6 | 6 | 5 | 最少 4 组 | ⏳ |
| E.g. 6 | 45 | 8 | 6 | 6 | 5 | 最少 10 组（at least 4） | ⏳ |
| E.g. 7 | 45 | 9 | 6 | 5 | 4 | 最少 3 组 | ⏳ |
| E.g. 8 | 45 | 10 | 6 | 6 | 4 | 最少 3 组 | ⏳ |
| E.g. 9 | 45 | 12 | 6 | 6 | 4 | 最少 6 组 | ⏳ |

## 🎯 加分项检查

- [x] 支持多种参数组合（m, n, k, j, s）
- [x] 方法好（回溯 + 贪心混合）
- [ ] 生成最优/接近最优解（需要测试验证）
- [x] 执行时间短且结果好
- [x] 能在手机上运行（PWA/响应式）
- [ ] 线性代数方法（未实现，可选）

## 📝 文档要求

### 用户手册
- [ ] 清晰、简洁、易懂
- [ ] 详细说明如何安装和执行系统
- [ ] 步骤说明
- [ ] 与项目报告分开

### 项目报告
- [ ] 方法（带流程图/图表）
- [ ] 开发的功能
- [ ] 贡献（运行时间短、结果最优）
- [ ] 遇到的问题
- [ ] 运行结果示例

## 🔍 下一步行动

1. **测试所有示例**：运行 E.g. 1-9，验证结果数量
2. **确认结果显示格式**：查看 PDF 中结果的具体格式
3. **实现分页功能**（如果需要）
4. **编写用户手册**
5. **编写项目报告**
