# 算法简要说明（Optimal Samples Selection）

## 1. 问题建模

本系统把任务建模为 **Set Cover（集合覆盖）**：

- 从 `m` 个元素中选出 `n` 个样本；
- 在这 `n` 个样本中选择若干个大小为 `k` 的组合；
- 满足由 `j`、`s`、`at least` 定义的覆盖约束；
- 目标是让选出的 `k` 组合数量尽可能少。

核心目标：**在满足覆盖规则前提下最小化组数**。

---

## 2. 分层求解策略

由于问题本质为 NP-Hard，系统采用“小规模精确 + 大规模近似”的双路线：

1. **Backtracking（精确法）**
   - 适用于较小搜索空间；
   - 能得到全局最优解（exact）。

2. **GRASP 系列（近似法）**
   - 适用于较大搜索空间；
   - 在时间预算内输出近优解（near-optimal）；
   - 支持三种模式：
     - `fast`：速度优先（超大规模）
     - `balanced`：默认平衡
     - `quality`：结果质量优先（更长预算）

---

## 3. 设计原因

系统目标不是单纯追求理论最优，而是兼顾：

- 课堂演示的稳定性与时延；
- 提交报告可解释性；
- 网页/本地模式下的可用性。

因此采用：

- 小规模给精确最优；
- 大规模给可控时间内的近优；
- 通过 `solveMode` 提供速度/质量可调。

---

## 4. 关键工程优化

- 覆盖关系预计算，减少重复计算；
- bitset 加速覆盖计数；
- 候选去重与冗余组移除（在合适规模下启用）；
- 超大空间采用随机采样扫描，降低全量遍历成本；
- `Save to history` 支持 `precomputed`，避免保存时重复求解。

---

## 5. 正确性与性能证据

系统提供可复现证据脚本：

- `scripts/generate-evidence-report.js`

输出内容包括：

- 覆盖率（covered/total `j` combinations）；
- 方法名称（`backtrack` / `grasp` / `grasp-fast` / `grasp-quality`）；
- 运行时间与组数统计；
- 最佳一次的结果组合。

示例证据文件位于：

- `submission/sample-runs/evidence-2026-04-14T06-15-31-460Z.md`
- `submission/sample-runs/evidence-n20-fast.md`
- `submission/sample-runs/evidence-n25-fast.md`

---

## 6. 一句话总结

本系统算法设计为：**小规模用回溯保证最优，大规模用 GRASP 在时间预算内给近优，并用证据报告验证正确性与性能。**
